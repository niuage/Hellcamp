$("#rooms > tbody > tr")
.append("<%= escape_javascript( render "rooms/room", :room => @room, :room_count => @room_count ) %>")
.show(function(){
$(".room-tab-<%= @room.id %>").remove_notifications();
$(window).resize();
});
